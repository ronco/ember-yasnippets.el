# -*- mode: snippet; require-final-newline: nil -*-
# name: Ember Observer
# key: observer
# binding: direct-keybinding
# --
${1:fooChanged}: Ember.computed('${2:dependencies}', function() {
${3:// body}
}),
